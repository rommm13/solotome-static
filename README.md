# SoloTome static assets

Public, non-sensitive static assets for SoloTome: PWA icons, web manifest and release metadata.

The application source remains in a separate private development repository until the production open-source release.
